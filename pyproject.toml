[tool.poetry]
name = "leantool"
version = "0.1.0"
description = ""
authors = ["Alok Singh <alokbeniwal@gmail.com>"]
readme = "README.md"

packages = [{ include = "LeanTool" }, { include = "leantool" }]

[tool.poetry.dependencies]
python = "^3.10"
litellm = "^1.61.7"
streamlit = "^1.40.1"
jsonlines = "^4.0.0"
pantograph = ">=0.3.1"
flask = "^3.1.0"
mcp = { extras = ["cli"], version = "^1.3.0" }

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
